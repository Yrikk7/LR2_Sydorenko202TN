#include"Header.h"
#include <iostream>

int main()
{
	float x, y, z;
	Vector3D one;
	one = one.Init(1, 1, 1, 1, 1, 1,1);
	one = one.Read(1, 1, 1, 1, 1, 1,1);
	one = one.multip();
	one = one.subtraction();
	one = one.scalar();
	one = one.multialar();
	one = one.comparisonvector();
	one = one.comparingthelengthofs();
};